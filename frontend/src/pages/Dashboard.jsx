import React from 'react';

export default function Dashboard(){
  return (
    <div style={{ padding: 20 }}>
      <h1>Pharmacy Dashboard</h1>
      <p>Welcome — this is the starter dashboard. Add inventory, reminders, and orders from the UI.</p>
    </div>
  );
}
